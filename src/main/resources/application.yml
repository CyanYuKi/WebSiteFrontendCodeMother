spring:
  application:
    name: WebsiteMother
  profiles:
    active: local
pexels:
  api-key: ${PEXELS_API_KEY:}

langchain4j:
  community:
    dashscope:
      chat-model:
        api-key: ${DASHSCOPE_API_KEY:}
        model-name: qwen-turbo